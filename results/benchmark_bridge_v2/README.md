# Benchmark bridge v2 results

No bridge inference has started.

This directory is reserved for exploratory cross-benchmark outputs under design ID `evisuff-benchmark-bridge-v2`.

A result is admissible here only when it records:

- the exact upstream commits and item manifests for FinAR-Bench, IPO Finance Agent, and BigFinanceBench;
- the exact evaluated model and returned model identifiers;
- the judge models and grading configuration;
- the tool surface used for each benchmark;
- evaluated-model calls separately from judge calls;
- per-item responses, traces, hashes, tokens, latency, and errors;
- the EviSuff condition and stateful round identifiers;
- a statement that the bridge pilot is exploratory and excluded from confirmatory V2 estimates.

The current launch lock remains closed because the FinAR test-file hash, exact FinAR task IDs, model IDs, judge models, tool surfaces, and budget have not yet all been frozen.
